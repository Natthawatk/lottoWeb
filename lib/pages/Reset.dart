import 'package:flutter/material.dart';

class Reset extends StatelessWidget {
  const Reset({super.key, required String status});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}